# Chroma-Deck

Run the CLI client (development):

```powershell
set HUB_URL=ws://localhost:8080
python chroma_deck.py
```

Commands:
- `/list` - request and display rooms
- `/join <room name>` - join a room and chat
- `/leave` - leave room
- `/quit` - exit client